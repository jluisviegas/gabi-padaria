import Container from '../components/Container';
import CartClient from './CartClient';

const CartPage = () => {
	return (
		<Container>
			<CartClient />
		</Container>
	);
};

export default CartPage;
